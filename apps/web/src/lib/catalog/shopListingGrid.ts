import { STOREFRONT_SHELL_CLASS } from "@/lib/layout/storefrontShell";

/** Shop category banner, toolbar, and grid — same shell as the header. */
export const SHOP_CATEGORY_PAGE_CLASS = STOREFRONT_SHELL_CLASS;

/** Shop category listing grid — one extra xl column so cards stay compact full-width. */
export const SHOP_CATEGORY_GRID_CLASS = "grid grid-cols-2 gap-4 sm:gap-5 md:grid-cols-3 md:gap-6 lg:grid-cols-4 lg:gap-6 xl:grid-cols-5 xl:gap-7";

export const SHOP_CATEGORY_SKELETON_CARDS = 15;
